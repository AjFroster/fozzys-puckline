import Placeholder from "./Placeholder";

export default function Model() {
  return (
    <Placeholder title="Methodology and track record" milestone="M8">
      The exact formulas, the current fitted parameters, the calibration curve, and running accuracy including the losses.
    </Placeholder>
  );
}
