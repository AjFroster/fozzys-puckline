import Placeholder from "./Placeholder";

export default function Slate() {
  return (
    <Placeholder title="Today's slate" milestone="M5">
      Every game today with a win probability, fair moneyline, model total, and P(over). Reads /data/v1/latest.json.
    </Placeholder>
  );
}
