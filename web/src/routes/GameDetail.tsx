import Placeholder from "./Placeholder";

export default function GameDetail() {
  return (
    <Placeholder title="Matchup" milestone="M5">
      One game in full — both Elo ratings, the pregame adjustments applied, expected goals per side, and the total distribution.
    </Placeholder>
  );
}
